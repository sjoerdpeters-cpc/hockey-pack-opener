# Dataset changelog


## v1.6.0 - 2026-05-02

- Applied rating model v1 to all players in `playerRatings.ts`.
- Added `ratingBreakdown` per player.
- Recalculated `rating` and automatic `rarity`.
- Added `RATING_MODEL.md`, `rating-summary.json` and `rating-breakdown.csv`.
- Updated `datasetMeta` to update sequence 11.
